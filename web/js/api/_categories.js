/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const categoriesAPI_auto = {

    /** Gets all entries from 'categories' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/categories`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'categories' by its primary key */
    getById: async function(categoryId) {
        let response = await axios.get(`${BASE_URL}/categories/${categoryId}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'categories' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/categories`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'categories' by its primary key */
    update: async function(formData, categoryId) {
        let response = await axios.put(`${BASE_URL}/categories/${categoryId}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'categories' by its primary key */
    delete: async function(categoryId) {
        let response = await axios.delete(`${BASE_URL}/categories/${categoryId}`, requestOptions);
        return response.data;
    },
};

export { categoriesAPI_auto };